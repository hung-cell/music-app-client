<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.assignment.musicapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".views.activity.TypeActivity"></activity>
        <activity android:name=".views.activity.ArtistListActivity" />
        <activity android:name=".views.activity.SearchActivity" />

        <activity android:name=".views.activity.LocalSongActivty" />
        <activity android:name=".views.activity.PlayerSongActivity" />
        <activity android:name=".views.activity.RegisterActivity" />
        <activity android:name=".views.activity.HomePageActivity">

        </activity>
        <activity android:name=".views.activity.LoginActivity" />
        <activity android:name=".views.activity.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>